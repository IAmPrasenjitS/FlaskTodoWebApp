<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
  <table>
    <tr>
      <th>ID</th>
      <th>Title</th>
      <th>Description</th>
    </tr>
    <!-- {{ users }} -->
    {% for x in users %}
    <tr>
      <td>{{x['id']}}</td>
      <td>{{x['title']}}</td>
      <td>{{x['des']}}</td>
    </tr>
    {% endfor %}
  </table>
</body>
</html>